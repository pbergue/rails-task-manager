<div class="margin-left">
  <h1>My tasks</h1>
  <% @tasks.each do |task| %>

    <%= task.completed ? "✔️" : "❌" %>
    <%= link_to task.title, task_path(task) %>
    <br>
  <% end %>
  <br><br>
  <%= link_to "➕ Add a new task", new_task_path %>
</div>
